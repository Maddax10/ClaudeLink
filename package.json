{
  "name": "claude-link",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Un canal de messages entre deux sessions Claude Code, sur deux machines, via un depot Git prive.",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "clink": "./dist/cli.js"
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.24.1",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.21.5",
    "memfs": "^4.17.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
